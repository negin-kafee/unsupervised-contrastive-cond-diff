# https://wandb.ai

wandb:
  _target_: pytorch_lightning.loggers.wandb.WandbLogger
  project: "contrastive_DDPM"
  entity: "your-wandb-entity"  # Update with your W&B entity
  name:  ${hydra:job.name}
  save_dir: "."
  offline: False # set True to store all logs only locally
  id: null # pass correct id to resume experiment!
  resume: False
  log_model: False
  prefix: ""
  job_type: ""
  group: ""
  tags: ["UAD", "contrastive_diffusion"]
